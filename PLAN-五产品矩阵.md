# 五产品矩阵 · 总体版本计划

> 版本：v1.0 ｜ 日期：2026-08-26
> 策略一句话：**一个内核，五个产品**——单仓库 `core + products/` profile 结构，
> 绝不复制五份代码；修 bug 修 core，五个产品同时受益。

---

## 〇、工程底座（所有产品的前置）

| 任务 | 内容 | 产出 |
|---|---|---|
| E1 仓库重构 ✅ | ~~现有代码归拢为 core~~ 已实现为：根目录扁平模块即共享内核（agent/llm/tools/ui/gpulocal…）+ `products/<name>/profile.json` 功能开关（gpulocal/dispatch/editor/voice/mcp/rag…），ui.py 已在功能入口接线 | `products/`：devtool / devtool_local / novelwriter / quant / devrag 五产品骨架 |
| E2 打包管线 ✅ | `packaging/build.py` 产品感知 PyInstaller（读 profile.json 决定 exe 名/数据目录/gpulocal 是否入包，排除 torch 等 AI 重栈）；CI `package` 任务三平台 matrix 自动出包上传 artifact；本地 Windows 冒烟通过（29MB exe 可启动） | 每个产品一条命令：`python packaging/build.py <product>`，产物 `dist/<exe>-<platform>/`（macOS 图标/签名属 E3） |
| E3 品牌体系 | 每产品独立：名称 / 图标 / 主题色 / 默认配置 / 帮助文案（profile.json 已有 title，图标/主题待补） | profile 目录即产品定义 |

---

## 产品 1：纯云端开发工具（无本地模型）

- **定位**：轻量、免费、三平台单文件的 AI 编程助手；自带 key、多 provider。
- **角色**：流量入口 + 口碑产品 + 分发管线验证器。不承担营收。
- **风险**：红海（Cursor/Continue/Cline/Aider），差异化 = 极简 + 免费 + 内置模型管理。

### 版本计划

| 版本 | 内容 | 验收标准 |
|---|---|---|
| v0.1 MVP（1–2 周） | core 裁剪：关闭 gpulocal 与调度本地侧；品牌 profile；三平台打包 | 三平台安装包可下可跑，冒烟测试过 |
| v0.2 | 打磨：会话管理、权限模式、附件/识图、帮助/欢迎卡（现有功能平移） | 新用户 5 分钟上手 |
| v1.0 | 官网/下载页、自动更新检查、崩溃日志上报（可选） | 公开发布 |

---

## 产品 2：本地模型省 token 开发工具（≈ 现有代码）

- **定位**：本地模型处理日常问答/草稿/路由，云端只做难事——省 token、离线可用、代码不出机。
- **角色**：技术护城河产品，与 gpulocal 互相成就。
- **灵魂功能**：调度路由准确率（本地够不上云端的判断）。误判 = 用户觉得"本地模型蠢"。

### 版本计划

| 版本 | 内容 | 验收标准 |
|---|---|---|
| v0.9 现状固化 | 现有功能全部进 core；gpulocal 三平台装机脚本收尾 | 现有 198 测试全绿 + 三平台 CI |
| v1.0 路由可度量 | 建路由评估集（100+ 条标注任务：该本地还是该云端）；误判率可见 | 评估报告可复跑，误判率 < 15% |
| v1.1 硬件门槛引导 | 装机检测（GPU/显存/内存）→ 推荐模型档位；一键下载 | 新机器 10 分钟跑起本地模型 |
| v1.2 成本面板 | 本地/云端 token 消耗与节省金额可视化 | 用户能说出"这个月省了多少钱" |

---

## 产品 3：短剧剧本 + 网文创作（无本地模型）

- **定位**：面向创作者（非开发者）的结构化写作生产工具。
- **核心价值**：prompt 工程与内容模板库，不是代码。
- **角色**：现金流产品；用户量大、付费意愿中等。

### 版本计划

| 版本 | 内容 | 验收标准 |
|---|---|---|
| v0.1 内核平移（1 周） | core 聊天/模型管理/会话/导出；作品=项目目录 | 能对话生成文本并保存 |
| v0.5 创作骨架（2–3 周） | 章节树面板、角色卡、世界观设定卡、风格模板、连载管理；导出 txt/docx | 一部短篇从大纲到正文全流程走完 |
| v1.0 模板库 | 短剧爆点结构（前 3 集钩子、付费卡点）、网文节奏模板（黄金三章、爽点密度）；模板可配置可分享 | 3 个真实创作者试用并出稿 |
| v1.1 | 敏感词/合规检查、多模型对比出稿、改写/扩写/续写指令 | 创作者留存周活 |

> ⚠️ 关键动作：开发期就找 2–3 个真实短剧/网文写作者共创模板，模板质量决定生死。

---

## 产品 4：量化开发工具（本地模型 + 微调）

- **定位**：量化策略开发/研究助手。卖点 = **策略不出机**（本地模型）+ 多平台对接 + 策略互转。
- **角色**：利润产品；用户小众但付费意愿最高。
- **合规红线**：定位开发/研究工具，永不承诺收益；下单动作强制人工确认（复用权限审批系统）；SDK 只引用不内嵌。

### 平台对接分层

| 层 | 平台 | 说明 |
|---|---|---|
| 第一批 | 聚宽 JoinQuant、PTrade | API 近乎同源（`initialize/handle_data`），互转最快出成果 |
| 基准层 | backtrader / qlib | 本地回测，做互转的语义校验基准 |
| 第二批 | 掘金量化 gm.api、QMT/miniQMT（xtquant） | QMT 本地终端探活/管理复用 gpulocal 思路 |
| 独立件 | 通达信/同花顺公式 → Python | 公式语言翻译器，单独模块 |
| 数据 | akshare / tushare / baostock | 研究数据接入 |

### 策略互转架构（铁律：IR，不做 N×N 直译）

```
源平台代码 → ① LLM 解析 → 策略中间表示 IR（JSON：universe/schedule/signals/orders/risk）
         → ② 模板发射（确定性，LLM 只兜底）→ 目标平台代码
         → ③ 校验：语法 + API 白名单 + backtrader 同数据信号一致率
```

### 版本计划

| 版本 | 内容 | 验收标准 |
|---|---|---|
| v0.1 骨架 ✅（1–2 周） | `products/quant/` 骨架：IR 定义、聚宽/PTrade API 对照表、双均线策略双向互转 demo（`python -m products.quant.demo`；emitters/parsers/validate/translate 四件套路通，19 测试例） | demo 双方向互转 + 语法校验过 ✅ |
| v0.5 ✅（2–3 周） | akshare 数据接入（`data/`，懒加载+CSV 缓存+自带样本行情）；语义校验用桩运行时替代 backtrader（`sim.py`：mocked API 真实 exec 生成代码逐日比对）；5 基准信号双向支持；RAG 知识库（`kb.py`+`llm_parse.py`：手写策略 LLM→IR 兜底，few-shot 从基准动态生成） | 互转信号一致率 ≥ 90%（5 个基准策略）→ **实测全部 100%**（`python -m products.quant.benchmark`）；手写策略经 LLM 兜底可解析 |
| v1.0 两平台互通 ✅（界面化部分） | 聚宽 ↔ PTrade 互转面板（`ui_panel_quant.py`：粘贴→选平台→互转→校验报告，LLM 兜底可选后台跑）+ 沙箱白名单校验内嵌 + QMT 终端探活（`qmt.py`，psutil/tasklist/ps 三级探测 + xtquant 检测）；顶栏入口仅在量化产品出现（新功能开关 `quant`） | 面板冒烟启动通过；「真实用户双平台跑同策略」待用户实测 |
| v1.5 掘金/QMT ✅ | gm.api、xtquant 适配器（emitters/parsers/sim 全链路四平台化；标的代码自动转换 `symbols.py`；QMT 下单为占位+人工确认注释，passorder 股数换算标 TBD 待核实）；互转面板与 RAG 知识库同步四平台 | 四平台互转矩阵 ✅：5 策略 × 12 方向全 100%（`python -m products.quant.benchmark`） |
| v2.0 微调版 | 语料（API 对照表 + 策略对）→ LoRA 微调 Qwen3-Coder → GGUF 转换 → 评估集卡线 | 本地模型转 IR 准确率 ≥ RAG 版，策略不出机 |

> ⚠️ 微调投入产出未验证前不重投入：v0.5 的 RAG 版先验证需求，微调是 v2.0 的升级而非起点。

---

## 产品 5：宁波易到救援开发平台（企业代码 RAG）

- **定位**：把**公司 N 个代码仓库 + 文档目录**建成一个持久化、可配置的知识库 RAG，
  开发对话时由**云端模型**调用（`gpulocal/dispatch` 关闭，纯云端版）。卖点 = 代码不出机 +
  跨仓库/文档秒级召回。
- **角色**：企业/团队内部开发提效产品；与产品 1/2 共用开发内核，靠 `rag` 功能开关呈现。
  **仅中文界面**（`zh_only` 开关，无中英切换）。
- **核心能力**（`codera.py` + `embed.py` + 面板）：
  - 多根目录（代码 + 文档）增量建进一个 SQLite 库，TF-IDF 余弦检索；
  - 可选 embedding 增强（配置了 embedding 模型才启用，混合评分；失败自动回退纯 TF-IDF）；
  - `kb_search` 工具 + 可开关的自动注入（提问自动检索 top 片段进上下文）。
- **检索方案**：TF-IDF 兜底 + 可选向量增强（与产品 4 的 RAG 哲学一致——诚实轻量起步，
  语料大了再上 embedding）。

### 版本计划

| 版本 | 内容 | 验收标准 |
|---|---|---|
| v0.1 骨架 | `products/devrag/` profile + `rag` 功能开关；`codera.py` 多根知识库 +
  `embed.py` 向量客户端；面板（根目录增删/建索引/开关/测试查询）；`kb_search` 工具 +
  自动注入；测试 `test_codera*.py` | 面板建索引 + 检索 + 工具全链路可用；测试全绿 |
| v0.5 | 检索质量：Top-k 重排、同义词/近义扩展、文档去重；embedding 混排调优 | 检索命中率可度量、报告可复跑 |
| v1.0 | 多会话共享知识库状态、索引后台自动增量、崩溃恢复；结果引用定位 | 跨会话一致、大型仓库索引可控 |

---

## 五、总路线图

```
第 1–2 周    E1 仓库重构 + 产品 1 v0.1（验证打包分发管线）
第 3–6 周    产品 3 v0.1→v0.5（创作者共创启动）；产品 1 v0.2
第 5–8 周    产品 2 v1.0 路由评估集；产品 3 v1.0 模板库
第 7–10 周   产品 4 v0.1→v0.5（互转 demo → RAG 版）
第 10 周后   产品 2 持续打磨；产品 4 v1.0；产品 5 v0.1→v0.5；按反馈决定微调投入
```

五条线不必串行——core 重构（E1）是唯一硬前置，之后按你点名的顺序开工。

---

## 六、风险登记册

| 风险 | 产品 | 缓解 |
|---|---|---|
| 开源编程助手红海 | 1 | 差异化文案前置想清楚；不投入获客预算 |
| 本地模型能力上限 / 路由误判 | 2 | 评估集 + 误判率度量；预期管理写进帮助 |
| 模板质量不足 | 3 | 真实创作者共创；先 3 个种子模板打磨 |
| 微调收益不成立 | 4 | RAG 先行，微调放 v2.0 再评估 |
| 金融合规 | 4 | 文案红线 + 下单强制确认 + 不碰代客理财 |
| QMT/PTrade 开户门槛挡用户 | 4 | 文档写清门槛；模拟盘路径完整可用 |
| 知识库召回不准 / embedding 依赖重 | 5 | TF-IDF 兜底零依赖；embedding 可选、失败回退；检索命中率可度量 |

---

## 七、开工口令（你通知我时说哪个就动哪个）

- 「开工底座」→ ~~E1 仓库重构~~ ✅ 已完成（products/ profile 机制 + 五产品骨架）
- 「开工产品 1」→ 纯云端版 profile + 打包
- 「开工产品 3」→ 创作工具 v0.1
- 「开工产品 5」→ ~devrag（企业代码知识库 RAG）v0.1 骨架 ✅ 已落地
- 「开工产品 4」→ quant 骨架 + 双均线互转 demo
- 产品 2 就是现在这条线，随时直接提需求即可
